@using FinalProject.ViewModels.List;

@{
    Layout = null;
}

@model ListMembersViewModel

@if (!Model.Users.Any())
{
    <div style="margin-top:40%; text-align:center;">
        <h5>
            This List is lonely
        </h5>
        <p style="color:gray;">
            People who follow this List will show up here.
        </p>
    </div>
}

@foreach (var user in Model.Users)
{
    <div class="follows-content">
        <button class="content no-border" style="padding:12px 0">
            <div class="profile">
                <div class="image">
                    @if (user.UserName != User.Identity.Name)
                    {
                        <a asp-action="Index" asp-controller="customuser" asp-route-userId="@user.Id">
                            <img src="/assets/images/ProfileImages/@user.ProfilePhoto"
                                 alt="Profile Icon">
                        </a>
                    }
                    else
                    {
                        <a asp-action="Index" asp-controller="profile">
                            <img src="/assets/images/ProfileImages/@user.ProfilePhoto"
                                 alt="Profile Icon">
                        </a>
                    }
                </div>

                <div class="names-follow-bio w-100">
                    <div class="names-follow" style="padding-left:12px;">
                        <div class="names">
                            <div class="username">
                                <span>@user.Name</span>
                                @if (user.IsPremium)
                                {
                                    <div class="verify-icon-blue">
                                        <i class='bx bxs-badge-check'></i>
                                    </div>
                                }
                                else if (user.IsVerified)
                                {
                                    <div class="verify-icon-yellow">
                                        <i class='bx bxs-badge-check'></i>
                                    </div>
                                }
                                else if (user.IsGovermentVerified)
                                {
                                    <div class="verify-icon-blue" style="color:#839aaa">
                                        <i class='bx bxs-badge-check'></i>
                                    </div>
                                }
                                else
                                {

                                }
                            </div>
                            <div class="name">
                                <span>@@@user.UserName</span>
                            </div>

                        </div>
                        @if (user.UserName != User.Identity.Name)
                        {
                            <div class="follow-button">
                                <a class="switch on follow follow-unfollow @(Model.Follow.FirstOrDefault(f => f.FollowerId == Model.CurrentUser.Id) == null ? "active" : "")" asp-action="Follow" asp-route-userId="@user.Id">
                                    <span>
                                        Follow
                                    </span>
                                </a>
                                <a class="switch off unfollow follow-unfollow @(Model.Follow.FirstOrDefault(f => f.FollowerId == Model.CurrentUser.Id) == null ? "" : "active")" asp-action="Follow" asp-route-userId="@user.Id">
                                    <span>
                                        Following
                                    </span>
                                </a>
                            </div>
                        }
                    </div>
                    <div class="bio">
                        <span>
                            @user.Bio
                        </span>
                    </div>
                </div>
            </div>
        </button>
    </div>
}




